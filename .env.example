# Server-only credentials for the Vite development proxy. Never prefix secrets with VITE_.
AZURE_SPEECH_KEY=replace-with-your-Azure-Speech-key
AZURE_SPEECH_REGION=replace-with-your-Azure-region

# Browser-safe optional override. It contains no secret and defaults to this path.
VITE_PRONUNCIATION_API_PATH=/api/pronunciation-assessment
